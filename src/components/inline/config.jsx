import React from 'react';
import './config.sass';

export default function EncryptedLabel() {
  return (
    <span className="inline-config__encrypted-label">
      Encrypted Value
    </span>
  );
}
